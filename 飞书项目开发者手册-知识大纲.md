# 飞书项目开发者手册 - 知识大纲

> 本大纲基于飞书项目帮助中心「开发者手册」分类(分类ID: 1p8d7djs)下的全部文章系统化整理而成。
> 入口URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/nh4exbsn

---

## 一、插件体系总览

### 1. 什么是插件

- **核心概念**：插件即飞书项目提供的开放能力。开发者可以根据业务场景创建插件来满足业务需求;企业可以通过插件功能为组织定制专属插件,从而使飞书项目与企业正在使用的各类软件、工具、平台等实现更紧密的联动。
- **关键功能/API**：
  - 自定义 UI 展示:通过丰富的点位构成和 UI 开放能力,开发者可以自定义功能嵌入至目标飞书项目空间各点位
  - 实现前后端功能交互:提供客户端 API、客户端组件以及 Open API,开发者可以自由设计前后端功能交互
  - MCP Server:基于插件体系,专为构建 AI Agent 打造的服务
- **开发要点**：
  - 插件开发完整生命周期:创建插件 → 添加构成 → 开发调试客户端 → 开发服务端 → 测试插件 → 发布插件
  - 若插件仅用于数据获取,可跳过添加构成和开发客户端步骤
  - 若插件不涉及与飞书项目间的服务端数据交互(OpenAPI),可跳过开发服务端步骤
- **注意事项**：
  - 飞书项目开放平台与众多合作伙伴提供了许多插件,在插件市场安装即可使用
  - 插件应用场景覆盖 Devops、文档管理、代码托管、工时管理等场景

### 2. 插件点位构成类型

- **核心概念**:插件的功能单元,定义插件在飞书项目页面中的展示位置和交互形式
- **关键功能/API** (完整点位构成清单):
  - **详情页 / Tab**:位于工作项实例页面的标签页处,支持自定义标签页名称和页面内容
  - **表格列控件 / 表单页控件 / 节点表单控件**:支持拖拽到表格列视图、详情页表单布局和节点表单布局中
  - **字段类型扩展字段**:扩展字段类型
  - **节点排期扩展字段**:扩展节点排期能力
  - **插件配置 / Configuration**:插件的配置管理页面
  - **实例更多按钮 / WBS-更多按钮 / 节点更多按钮 / 新建按钮 / 节点流转按钮 / 批量操作按钮**:各类按钮点位
  - **监听事件 / Listen**:实时接收飞书项目平台产生的各类重要通知
  - **拦截事件 / Intercept**:允许开发者干预飞书项目相关操作的事件能力
  - **触发器 / Trigger**:通过 HTTP 方式调用平台指定接口,触发规则执行
  - **操作 / Operation**:自动化规则的操作节点
  - **组件 / Component**:轻应用组件,为轻应用扩展自定义的三方组件
- **开发要点**:各点位构成支持网页端和移动端(部分),需分别定义 resourceId

### 3. 官方插件类型

- **工时资源管理插件**:帮助企业将人力资源成本要素变得可视、可控、可优化
- **OKR 集成插件**:通过飞书项目内的关联能力完成从目标到具体项目、需求的拆解及跟进
- **Gitlab 集成插件**:将 GitLab 上的研发活动和飞书项目的工作项进行关联
- **会议管理插件**:将分散在飞书的日程、会议、妙计之中的会议集中管理
- **Jira Issue 数据同步插件**:将 Jira Issue 信息同步到飞书项目
- **工时管理插件**:用于工时录入、工时审批、工时查询和工时统计分析场景

---

## 二、插件开发全流程

### 1. 环境准备

- **核心概念**:飞书项目开放平台基于 Web 前端技术体系设计并提供外部开发能力
- **关键功能/API**:
  - **知识储备**:了解 JavaScript、HTML、CSS 等语言;推荐了解 React 前端框架
  - **开发客户端环境**:
    - 安装 Node.js(版本需要高于 16.0.0,推荐 v18.20.5)和 NPM 包管理器
    - 安装飞书项目开发者工具 Lark Project CLI:`npm i -g @lark-project/cli`
    - 验证安装:`lpm --version`
    - 请勿使用 Yarn / CNPM 包管理工具
    - 大陆镜像源安装:`npm install -g @lark-project/cli@latest --registry https://registry.npmmirror.com`
  - **开发移动端**:需安装飞书移动端应用 7.35.x 及以上版本
  - **开发服务端环境**:对后端开发语言没有特定限制,官方提供了 GO、Java 的 SDK 封装
  - **浏览器**:推荐安装 Google Chrome、Microsoft Edge
- **开发要点**:
  - SDK 类型:
    - Golang: project-oapi-sdk-golang (v1.0.4, Golang >= 1.15)
    - Java: project-oapi-sdk-java (1.0.22, Java >= 1.8)
  - SDK 能力:结构化封装 OpenAPI 请求/响应参数;支持插件访问凭证的生命周期管理
- **代码/配置示例**:
  ```xml
  <!-- Java SDK Maven 配置 -->
  <settings>
    <profiles>
      <profile>
        <id>lark-project</id>
        <activation>
          <activeByDefault>true</activeByDefault>
        </activation>
        <repositories>
          <repository>
            <id>lark-project</id>
            <url>https://artifacts-cn-beijing.volces.com/repository/meego/</url>
          </repository>
        </repositories>
      </profile>
    </profiles>
  </settings>
  ```
  ```xml
  <!-- Java SDK 依赖 -->
  <dependency>
    <groupId>com.larksuite.project</groupId>
    <artifactId>oapi-sdk</artifactId>
    <version>1.0.22</version>
  </dependency>
  ```
- **注意事项**:跨域处理需要配置 CORS 策略,允许来自飞书项目的请求

### 2. 创建插件

- **核心概念**:在飞书项目开放平台创建插件并完成基本配置
- **关键功能/API**:
  - **创建插件**:访问飞书项目开放平台 → 单击+添加插件 → 编辑插件名称和图标 → 单击确定
  - **查看基本信息**:
    - 插件凭证:Plugin ID 和 Plugin Secret(用于开发调试,请勿泄露)
    - 基本信息配置:国际化配置、插件图标、名称、短描述、插件分类、插件详情、封面图、帮助文档
    - 添加协作人:支持升级负责人和移除协作人
    - 删除插件:仅插件负责人可以删除
  - **基础信息字段说明**:
    - 国际化配置:支持中文、英文、日文,支持多选
    - 插件图标:支持 SVG、PNG、JPEG 格式,大小不超过 2 MB,清晰度不低于 240*240 px
    - 插件短描述:不超过 100 字
    - 插件分类:最多可选 3 个分类
    - 插件详情:需要超过 50 字说明
    - 封面图:市场插件必选,比例 2:1,不小于 1600px*800px,不超过 10 MB
- **开发要点**:创建完成后,插件状态为"待上线"
- **注意事项**:删除插件前需先联系对应空间管理员先在自己空间删除此插件

### 3. 添加插件功能(构成)

- **核心概念**:根据插件功能,选择并添加合适的构成(如表单页控件、详情页、拦截事件等)
- **关键功能/API**:
  - 在左侧导航栏选择插件功能 → 单击+添加构成 → 选择构成类型 → 编辑配置信息
  - 通用配置项:名称、描述、Key(系统自动生成)、工作项类型、适用端、ResourceId
  - ResourceId:开发者自行定义,每个构成的客户端代码资源对应唯一的 ResourceId
- **开发要点**:
  - 初始化工程后平台会将插件构成 ResourceId 自动写入 plugin.config.json 文件
  - 若 ResourceId 不一致,需在控制台复制 ResourceId 至文件中
- **注意事项**:不同构成类型的配置项略有不同

### 4. 开发调试客户端

- **核心概念**:编写和调试插件的前端界面和交互逻辑
- **关键功能/API**:
  - **开启本地调试**:在飞书项目开放平台插件详情页勾选开启本地调试
  - **启动插件**:执行 `lpm start` 命令启动功能
  - **查看构成展示**:访问飞书项目空间查看构成客户端展示
  - **客户端代码开发**:在 feature 目录下进行插件客户端代码开发
  - **配置服务端域名**:若插件需要与后端服务通信,需配置正确的服务端域名
- **开发要点**:
  - 启动成功后会返回 webpack-dev-server 运行信息
  - 支持热更新,修改代码后页面自动刷新生效
  - 在飞书项目页面中,开启插件本地调试后需刷新页面才能看到渲染结果
- **代码/配置示例**:
  ```typescript
  // App.tsx 基础示例
  import { ReactElement } from 'react';
  function App(): ReactElement {
    return <div>hello world</div>;
  }
  export default App;
  ```

### 5. 开发服务端

- **核心概念**:开发用于处理数据和业务逻辑的后端服务
- **关键功能/API**:
  - 配置 Maven(Java)或 Go SDK
  - 获取访问凭证:Plugin ID、Plugin Secret、user_key
  - 跨域处理:配置 CORS 策略
  - 部署服务端工程
- **开发要点**:
  - 服务端工程需打包部署至服务器并启动
  - 需要配置正确的服务端域名
- **代码/配置示例** (Java Spring Boot CORS 配置):
  ```java
  @Slf4j
  @Configuration
  public class WebMvcConfiguration implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
      registry.addMapping("/**")
        .allowedOrigins("*")
        .allowedMethods("GET", "POST", "DELETE")
        .allowedHeaders("*");
    }
  }
  ```
- **注意事项**:由于插件客户端页面是从飞书项目域加载的,直接向后端服务发起请求会遇到跨域问题

### 6. 测试插件

- **核心概念**:进行端到端的联调测试,确保插件功能正常
- **关键功能/API**:访问飞书项目客户端构成展示页面,测试插件客户端是否能接收服务端数据
- **开发要点**:测试过程中,若服务端接口调用存在问题,请参见 Open API 调用咨询

### 7. 发布插件

- **核心概念**:打包版本,然后在开发者后台选择插件产物版本并发布
- **关键功能/API**:
  - **上传客户端版本**:执行 `lpm release` 命令上传插件产物版本
  - **发布插件**:
    - 在插件详情页选择管理 > 插件发布
    - 单击+创建版本
    - 编辑版本号、版本描述、选择产物版等信息
    - 设置插件可见范围(企业内使用 / 共享到插件商店)
    - 设置插件商店可见范围
    - 单击申请发布
  - **版本配置项**:
    - 版本号:当前发布的插件版本号
    - 版本描述:当前版本所包含的功能项及更新日志
    - 产物版本:客户端代码完成版本上传后可选择
    - 插件使用范围:企业内部使用 / 共享到插件商店
    - 插件商店可见范围:租户内所有空间插件商店可见 / 本企业内指定空间插件商店可见 / 不可在插件商店查看本插件
- **开发要点**:
  - 插件发布成功后,可见范围内空间的空间管理员即可在插件商店搜索并使用
  - 验证步骤:管理员安装 → 配置与使用 → 最终验证
- **注意事项**:若要发布为 ISV 插件,需经过租户管理员及平台管理员审批

---

## 三、客户端开发

### 1. 客户端开发概述

- **核心概念**:飞书项目开放平台提供了丰富的客户端 API 和组件帮助开发者快速开发客户端
- **关键功能/API**:
  - **客户端 API 介绍**:包含上下文、通用能力和数据模型三类
    - 上下文:提供当前环境上下文、当前插件功能所在的上下文等
    - 通用能力:提供端能力,如页面路由操作、打开模态框等
    - 数据模型:提供数据获取能力,如空间基础信息查询、工作项查询等
  - **客户端组件介绍**:分为基础组件和业务组件两类
    - 基础组件:提供了基础的界面组件,例如气泡卡片 / Popover
    - 业务组件:提供了平台内特有的业务能力,例如用户选择器 / UserSelect、工作项筛选器 / WorkItemFinder 等
  - **客户端限制**:
    - 资源限制:插件有一些限制无法随着应用使用量进行扩展
    - 使用限制:插件发布后,其客户端产物运行在飞书项目开放平台提供的沙箱环境中
- **开发要点**:开发客户端推荐流程:
  1. 安装开发者工具
  2. 初始化客户端
  3. 开发调试工程
  4. 发布插件产物
- **注意事项**:开发者可通过官方插件快速、直观地体验 API 的实际运行效果

### 2. 客户端 API 简介

- **核心概念**:飞书项目插件为插件开发者提供了一套客户端 API,通过客户端 API 可以方便的调用端能力,获取上下文来实现插件业务逻辑
- **关键功能/API**:
  - 通过 `window.JSSDK` 使用客户端 API,在插件中无需任何配置即可直接使用
  - 三大类 API:
    - **上下文 / Context**:提供当前环境上下文、当前插件功能所在的上下文等
    - **通用能力**:提供端能力,如获取当前登录用户的授权码、页面路由操作、打开模态框等
    - **数据模型**:提供数据获取能力,如空间基础信息查询、工作项查询等
- **开发要点**:开发者可通过官方插件快速、直观地体验 API 的实际运行效果

### 3. 使用客户端组件库

- **核心概念**:客户端组件库是插件在运行时加载到运行环境的一种注入资源
- **关键功能/API**:
  - **组件库类型提示**:安装 `@lark-project/ui-kit-plugin` 包
  - **消费方式**:
    - 网页端:`import { xxx } from '@lark-project/ui-kit-plugin';`
    - 移动端:`import { xxx } from '@lark-project/ui-kit-plugin/mobile';`
  - **组件库初始化**:依赖插件开发者注入共享模块 React & ReactDOM
  - **完整组件库使用流程**:配置主入口文件 → 配置共享资源 → 配置应用主体
- **开发要点**:
  - 所有消费组件库的位置都应该在 lazy App 的后面,包括引用的方式
  - 允许的包路径:`@lark-project/ui-kit-plugin`(网页端)、`@lark-project/ui-kit-plugin/mobile`(移动端)
- **代码/配置示例**:
  ```typescript
  // 安装
  npm i @lark-project/ui-kit-plugin@latest -D

  // 网页端消费方式
  import { xxx } from '@lark-project/ui-kit-plugin';

  // 移动端消费方式
  import { xxx } from '@lark-project/ui-kit-plugin/mobile';

  // 组件库初始化
  import React from 'react';
  import ReactDOM from 'react-dom';
  export function initShared() {
    return window.JSSDK.shared.setSharedModules({
      React,
      ReactDOM,
    });
  }

  // 使用示例
  import { WorkItemFinder } from '@lark-project/ui-kit-plugin';
  function App(): ReactElement {
    const [searchId, setSearchId] = useState<string>();
    return (
      <WorkItemFinder
        spaceId={spaceId}
        workObjectId={workObjectId}
        searchId={searchId}
        onSubmit={(val) => setSearchId(val.searchId)}
      />
    );
  }
  ```
- **注意事项**:
  - 请勿尝试使用 `@lark-project/ui-kit-plugin/xxx` 等路径用法来消费内部组件/方法
  - 仅确保导出的类型与组件稳定可靠

### 4. 详情标签页 / Tab 开发

- **核心概念**:位于工作项实例页面的标签页处,开发者支持自定义标签页名称,同时支持自定义该标签页下独立页面内的所有样式及内容
- **关键功能/API**:
  - **适用场景**:项目进展记录、用例执行记录管理、需求合码情况管理等
  - **适用端**:支持网页端、移动端两种开发场景
  - **JSSDK API**:
    - `window.JSSDK.tab.getContext`:详情标签页上下文
    - `window.JSSDK.WorkObject.load`:工作项配置
    - `window.JSSDK.WorkItem.load`:工作项
    - `window.JSSDK.storage.getItem`:本地缓存
- **开发要点**:
  - 开发流程:添加构成 → 开发客户端 → 查看渲染效果
  - 配置入口文件 `plugin.config.json`,添加 id 为 `tab_web_demo` 的入口配置
  - 在 `entry` 对应的文件位置书写入口代码
- **代码/配置示例**:
  ```typescript
  // App.tsx 入口组件
  import { FieldManagementPage } from "../../pages";
  const App: React.FC = () => {
    return <FieldManagementPage />
  }
  export default App;

  // 页面布局
  import { Select } from "@lark-project/ui-kit-plugin";
  export const FieldManagementPage = () => {
    const { selectFieldType, setSelectFieldType, groupFieldList, currentFieldsMap, loading } = useModel();
    return (
      <div className="wrapper">
        <Select
          optionList={optionList}
          style={{ width: 320 }}
          placeholder="请选择字段类型"
          value={selectFieldType}
          onChange={(value: string) => setSelectFieldType(value)}
        />
        <ColumnPreview
          spinning={loading}
          groupFieldList={groupFieldList}
          currentFieldsMap={currentFieldsMap}
        />
      </div>
    );
  };
  ```
- **注意事项**:使用 `@douyinfe/semi-ui` 和 `@lark-project/js-sdk` 等依赖

### 5. 控件 / Control 开发

- **核心概念**:控件是由开发者自定义的页面表单组件
- **关键功能/API**:
  - **页面入口**:支持拖拽到表格列视图、详情页表单布局和节点表单布局中
  - **适用场景**:插件功能类型中使用概率最高的一种,应用场景最接近"字段"
  - **适用端**:
    - 详情页/节点表单控件:支持网页端和移动端双端开发(V7.36.0+)
    - 表格列控件:支持网页端开发
  - **控件位置**:详情页表单控件、节点表单控件、表格列控件
  - **表格列控件实现方式**:不支持在客户端运行自定义脚本,仅支持通过 API 接口计算返回用于渲染预置的布局配置
  - **布局配置**:包含变量申明 definitions、布局模板 template
  - **物料/标签**:view(容器类)、popover(容器类)、text(展示类)、image(展示类)
  - **变量申明类型**:自定义变量、自定义色值、多语言词条
  - **表达式**:使用 `{{expression}}` (Mustache 语法)
- **开发要点**:
  - **表单控件网页端**:仅支持自定义渲染模式,可以以开发 Web 站点的经验来开发
  - **新建页适配**:飞书项目不支持存储插件控件值,需要额外实现控件值的存储逻辑
  - **新建页回调地址协议**:平台会向开发者后台配置的 Webhook 回调地址发起网络请求
  - **移动端控件**:必须同时开发网页端控件,有两种状态:概览和详情
  - **表格列控件性能要求**:单次请求响应事件小于 3s,单次请求响应体体积小于 100kB
- **注意事项**:
  - 控件不支持筛选、排序、分组、度量
  - 不支持仅移动端控件,因为控件需要通过管理员先行配置到表单

### 6. 轻应用组件 / LiteAppComponent

- **核心概念**:开放平台支持通过插件机制为轻应用扩展插件自定义的三方组件
- **关键功能/API**:
  - 一款插件至多支持添加 20 个组件构成
  - **配置项**:组件名称、描述、Key、组件图标、适用端、resourceId、组件尺寸、组件属性、输出属性
  - **组件尺寸模式**:栅格、固定值两种
  - **组件属性类型**(10种):文本、数字、开关、单选、多选、日期和时间、日期范围、视图、实例、字段、工作项类型、字段、数据源
  - **输出属性类型**(8种):文本、数字、日期和时间、日期范围、视图、实例、工作项类型、数据集
  - **属性分类**:基础属性、业务属性、系统属性
- **开发要点**:
  - 组件属性在插件构成声明后,其配置面板无需开发者实现,系统会自动生成
  - 所有属性在管理员配置时都是选填的,并且支持属性值来自对当前页面其他组件的订阅
- **注意事项**:组件能力目前只适用于网页端

---

## 四、服务端开发

### 1. Open API 概述

- **核心概念**:飞书项目开放平台提供了丰富的 API 接口,开发者可以通过 API 接口实现插件与飞书项目的数据交互
- **关键功能/API**:
  - Open API 介绍及调用流程
  - Open API 鉴权
  - Open API 列表
  - 服务端 SDK(Go、Java)
  - Open API 错误码
  - Open API 调用咨询
- **开发要点**:
  - 调用 API 过程中遇到问题可参见 Open API 错误码、Open API 调用咨询文档排查解决
  - 跨域处理:由于插件在飞书项目页面中发起请求时会出现跨域问题
- **注意事项**:若插件包含付费逻辑,需要在代码中实现这个逻辑

### 2. 获取访问凭证

- **核心概念**:要调用飞书项目开放平台 OpenAPI 前,插件需要获取对应的访问凭证
- **关键功能/API**:
  - **访问凭证类型**:
    - `plugin_access_token`:插件访问凭证,用于获取用户访问凭证或者调用 OpenAPI(需要与 X-User-Key 作为用户信息搭配使用)
    - `virtual_plugin_token`:虚拟插件访问凭证,仅供开发调试使用
    - `user_access_token`:用户访问凭证,代表了用户对插件的临时授权
  - **使用访问凭证的两种方式**:
    - 方式一:将 `user_access_token` 添加到 HTTP 请求中的 `X-Plugin-Token` 标头中
    - 方式二:将 `plugin_access_token` 添加到 HTTP 请求中的 `X-Plugin-Token` 标头中,并将访问者 UserKey 添加到 `X-User-Key` 标头中
  - **获取插件访问凭证**:
    - 请求方式:POST
    - 请求地址:`https://{域名}/open_api/authen/plugin_token`
    - 请求参数:`plugin_id`、`plugin_secret`、`type`(可选,0 或 1)
  - **获取用户访问凭证**:
    - 获取授权码 code(有效期 5 分钟,仅可使用一次)
    - 通过授权码 code 和 plugin_access_token 获取 user_access_token
    - 请求方式:POST
    - 请求地址:`https://{域名}/open_api/authen/user_plugin_token`
    - 请求参数:`code`、`grant_type`(必须填写 "authorization_code")
  - **刷新用户访问凭证**:
    - 请求方式:POST
    - 请求地址:`https://{域名}/open_api/authen/refresh_token`
    - 请求参数:`refresh_token`、`type`(必须填写 "1")
  - **客户端验证用户授权和登录态**:
    - 使用 `window.JSSDK.utils.getAuthCode()` 获取授权码
    - 通过 `window.JSSDK.storage.setItem()` 存储 token
- **开发要点**:
  - `plugin_access_token` 的有效期为 7200 秒(2 小时),有效期内重复获取会返回相同结果
  - 开发者需要缓存 `plugin_access_token`,用于后续接口的调用
  - 使用 `user_access_token` 的前提是插件有添加插件构成
  - 不能直接在前端插件直接调用 OpenAPI,需要在自己 的服务端发起请求
  - 在开发、调试过程中使用 `virtual_plugin_token`;插件发布后使用 `plugin_access_token`
- **代码/配置示例**:
  ```bash
  # 获取插件访问凭证
  curl --location 'https://{域名}/open_api/authen/plugin_token' \
    --header 'Content-Type: application/json' \
    --data '{
      "plugin_id": "MII_63E9D49B8C82****",
      "plugin_secret": "D01B5F1A191C8620D133CDC371C0****",
      "type": 0
    }'
  ```
  ```typescript
  // 客户端获取授权码
  const { code } = await window.JSSDK.utils.getAuthCode();
  console.log('code', code); // "27s72sn7s"
  ```
- **注意事项**:决定 OpenAPI 是否允许被调用或相关数据是否被返回受以下因素影响:
  1. 插件是否申请对应权限
  2. 对应空间是否安装拥有对应权限的插件版本
  3. 用户是否拥有对应空间或数据的访问权限或编辑权限
  - 若使用 `virtual_plugin_token`,将不受第 2 点限制,但 Open API 仅支持获取被设置为数据范围的空间内的数据

### 3. 服务端 SDK

- **核心概念**:为了帮助开发者更轻松地调用 Open API 并降低开发成本,飞书项目提供了 Go 和 Java 语言的服务端 SDK
- **关键功能/API**:
  - **SDK 优势**:
    1. 将 OpenAPI 的请求和响应参数封装为结构化对象,简化了调用过程
    2. 自动管理插件访问凭证的生命周期,无需自行维护
  - **语言类型**:
    - Golang: project-oapi-sdk-golang (v1.0.28, Golang >= 1.15)
    - Java: project-oapi-sdk-java (1.0.30, Java >= 1.8)
  - **安装 SDK**:
    - Golang: `go get -u github.com/larksuite/project-oapi-sdk-golang`
    - Java: 在 settings.xml 和 pom.xml 中添加配置
- **开发要点**:Java SDK 1.0.22 版本新增功能:
  - 工作项(Work Item)功能增强:支持按组更新复合字段,并扩展 WBS 视图功能
  - 新增了 updateCompoundFieldValue 接口
  - WBS 视图的请求支持 need_union_deliverable 和 need_schedule_table_agg 查询参数
- **代码/配置示例**:
  ```bash
  # Golang SDK 安装
  go get -u github.com/larksuite/project-oapi-sdk-golang
  ```
  ```xml
  <!-- Java SDK pom.xml 依赖 -->
  <dependency>
    <groupId>com.larksuite.project</groupId>
    <artifactId>oapi-sdk</artifactId>
    <version>{version}</version>
  </dependency>
  ```

---

## 五、事件能力

### 1. 监听事件 / Listen

- **核心概念**:飞书项目监听事件功能是飞书项目开放平台推出的一项服务,允许开发者在自己开发的插件中,实时接收到飞书项目平台产生的各类重要通知
- **关键功能/API**:
  - **事件分类**:
    - 工作项(100):创建工作项(1001)、删除工作项(1002)、恢复工作项(1004)、终止工作项(1005)、模版升级(1006)、修改字段(1009)
    - 节点(200):完成节点(2001)、删除节点(2002)、恢复节点(2003)、回滚节点(2004)、修改估分与排期(2005)、修改节点负责人(2006)、修改实际工时(2007)、多人确认节点流转事件(2009)
    - 状态(300):状态变更(3001)
    - 子任务(400):新建子任务(4001)、删除子任务(4002)、完成子任务(4003)、取消完成子任务(4004)、修改子任务负责人(4005)、修改子任务估分(4006)、修改子任务排期(4007)、修改子任务实际工时(4008)、修改子任务字段(4009)
    - 空间(500):添加空间成员(5001)、删除空间成员(5002)、完成工作移交(5003)、拒绝工作移交(5004)
  - **配置流程**:
    1. 配置监听事件:添加插件需监听的工作项类型及其对应事件类型
    2. 配置回调地址:需公网可以访问,支持热更
    3. 配置回调 token(可选):配置后签名中包含该 token
    4. 版本发布:需要创建版本并发布
    5. 接收事件:用户安装插件后发生相关数据变化会推送事件
  - **事件结构**:
    - `log_id`:日志 ID
    - `source_plugin_version`:插件版本
    - `request_time`:事件发送时间(毫秒级时间戳)
    - `signature`:请求签名
    - `source`:来源类型(normal、openapi、system、automation)
    - `source_plugin_id`:来源插件 ID
    - `source_plugin_name`:来源插件名称
    - `data`:事件详细信息
    - `data.event_type`:事件类型编码
    - `data.idempotent_key`:事件唯一标识
- **开发要点**:
  - **重试机制**:发送失败后会进行 4 次重试(立即重试、1 分钟、2 分钟、7 分钟)
  - **事件顺序**:相关事件的发送为无序发送,可通过 `request_time` 判断事件发生顺序
  - **幂等性**:同一事件可能多次发送,可通过 `idempotent_key` 过滤
  - **实时性保证**:监听事件基本保证 openAPI 回查实时性;视图操作查询会有 2 秒延迟;获取指定的关联工作项列表(单空间)会有 1 分钟延迟
- **注意事项**:
  - **稳定性要求**:在接收到飞书项目的请求后,需要在 1000ms 内返回状态码为 200 的响应
  - **优化策略**:
    1. 将耗时操作异步处理
    2. 使用位于中国大陆的服务器
  - 批量操作引发的数据变化,会发送对应的多个数据变更事件

### 2. 拦截事件 / Intercept

- **核心概念**:拦截事件是一种允许开发者干预飞书项目相关操作的事件能力,如拦截新建、修改字段等
- **关键功能/API**:
  - **交互形式**:插件订阅相关事件后,用户操作时将向开发者后端发送事件,开发者在约定时间内返回是否需要干预
    - 如果插件干预,将拦截用户的操作,并将开发者后端的回调信息透传回用户端
    - 如果插件不干预/或未在约定时间返回结论,则放行用户本次操作
  - **适用场景**:针对用户操作权限的精细化管控
  - **适用端**:支持网页端、移动端两种开发场景
  - **配置项**:拦截事件名称、描述、工作项类型、触发事件类型、回调接口、回调token、适用端、resourceId
  - **拦截事件枚举**(EInterceptEventType):
    - 创建工作项(1001)、删除工作项(1002)、批量删除工作项(1003)、恢复工作项(1004)、终止工作项(1005)、模板升级(1006)、批量模板升级(1007)、导出工作项(1008)、修改字段(1009)、批量修改字段(1010)、发布草稿(1011)、创建草稿(1012)
    - 完成节点(2001)、删除节点(2002)、恢复节点(2003)、回滚节点(2004)、填写排期与估分(2005)、修改节点负责人(2006)
    - 变更状态(3001)
    - 新增子任务(4001)、删除子任务(4002)、完成子任务(4003)、取消完成子任务(4004)、修改子任务负责人(4005)、修改子任务估分(4006)、修改子任务排期(4007)、修改子任务实际工时(4008)
  - **拦截弹窗**:`intercept_options.disable_show_plugin` 为 true 不会唤起拦截弹窗
  - **数据通讯能力**:`window.JSSDK.intercept.getContext()` 获取拦截事件弹窗所在的上下文信息
- **开发要点**:
  - 当插件选择拦截后,飞书项目将不继续执行用户操作的后续逻辑
  - 强烈建议在插件设计时,考虑拦截后的引导衔接行为
  - 一个插件支持订阅多个拦截事件
  - 当一个空间包含多个含有同拦截点位的插件时,系统会按插件安装的先后顺序生效
- **代码/配置示例**:
  ```typescript
  // 拦截事件枚举
  enum EInterceptEventType {
    CreateWorkItem = 1001,
    DeleteWorkItem = 1002,
    EditField = 1009,
    FinishNode = 2001,
    UpdateState = 3001,
    NewSubTask = 4001,
    // ... 更多事件类型
  }

  // 拦截弹窗配置
  {
    "need_intercept": true,
    "msg": { "zh": "", "en": "", "ja": "" },
    "custom_data": "",
    "intercept_options": {
      "disable_show_plugin": true
    }
  }

  // 获取拦截事件上下文
  const interceptContext = await window.JSSDK.intercept.getContext();
  console.log(interceptContext.eventType); // 1001
  ```

### 3. 自动化连接器 / Connector

- **核心概念**:自动化连接器是一种开发者扩展自动化流程规则的开放能力,支持空间管理员将插件触发器/操作与平台内置触发器/操作进行连接
- **关键功能/API**:
  - **功能类别**:
    1. 自动化开放能力包含触发器、条件和操作
    2. 触发器/操作规则表单开放
    3. 自动化连接器插件配置支持热更新
  - **自动化触发器**:
    - 配置项:触发器名称、触发器描述、触发器key、webhook推送至、token、触发器入参、触发器出参
    - 规则订阅事件(RuleSubscribeEvent):规则配置者保存规则后,平台向 Webhook 推送事件
    - 开发者推送事件规范:开发者向触发器API发送请求触发自动化规则执行
      - 请求方式:POST
      - 请求地址:`/webhook-api/auto/trigger/data`
      - 查询参数:`project_key`、`plugin_key`、`trigger_key`
      - 请求头:`X-ITEM-UUID`(幂等key)、`X-TOKEN`(token签名)
  - **自动化操作**:
    - 配置项:操作名称、操作描述、操作key、适用的触发器类型、webhook推送至、token、操作入参
    - 平台事件:规则订阅事件(RuleSubscribeEvent)
    - 工作项事件:WorkitemCreateEvent、WorkitemUpdateEvent、WorkitemAbortedEvent、WorkitemRestoreEvent、WorkitemStatusEvent、TaskStatusEvent、TaskUpdateEvent、WorkitemPersonAssignedEvent、WorkFlowNodeStatusEvent、WorkFlowNodeScheduleEvent、WorkitemCommentEvent、WorkitemFinishEvent、WorkitemDeleteEvent、TimedTriggerEvent、DynamicTimeTriggerEvent
  - **响应码**:
    - 0:成功
    - 1001:qps限制
    - 1002:大小超过限制
    - 1003:幂等
    - 1004:签名错误
    - 1005:黑名单限制
    - 2001:参数异常
    - 2002:缺乏必填参数
    - 5001:系统错误
  - **入参/出参 JSON 格式规范**:支持 field_type(select、radio、multi_select 等)、form_config(label、placeholder、validate)、field_attr.option(enum、remote)
- **开发要点**:
  - **面向插件开发者的亮点**:
    1. 降低 OpenAPI 调用量
    2. 降低插件运维成本
    3. 提升插件扩展性
  - **面向空间管理员的亮点**:
    1. 数据集成规则统一管理
    2. 提升插件易用性
  - 一个插件支持开发多个自动化触发器/操作,配置互相独立生效
  - 表单规则或 Webhook 地址变化时,无需发版即可实时更新(新增或删除功能点位仍需发版)
- **注意事项**:
  - 触发器入参/出参字段 key 用于用户自定义唯一标识字段,不可重复
  - 操作入参的 required 语义:规则配置者必须指定该字段的值

---

## 六、MCP Server

### 1. 插件体系 MCP Server 介绍

- **核心概念**:飞书项目 MCP (Model Context Protocol) 是一项基于 MCP 标准协议的官方服务,专门优化了其工具(Tool)的输入和输出参数,使 AI 在读写飞书项目数据时能更好地适配自然语言对话场景
- **关键功能/API**:
  - **启用插件 MCP Server**:在插件发布时启用
  - **MCP Server URL 格式**:`https://project.feishu.cn/mcp_server/v1?mcpKey=aaaaa-bbbb-xxxx&userKey=<userKey>`
  - **URL 查询参数**:
    - `userKey`:用户身份标识,配置后所有写入操作都将以该用户的身份执行
  - **传输方式**:Streamable HTTP
  - **MCP Tools 分类**(覆盖实例操作、节点操作和常用查询等场景):
    - **空间与人员**:获取空间基础信息、获取空间下的工作项类型、查询用户基础信息、获取空间下的关联关系、查看团队列表、查询团队成员信息
    - **工作项实例**:获取空间下的字段配置、获取工作项节点字段信息、获取空间下角色配置信息、获取工作项实例信息、获取工作项元信息、创建工作项、更新工作项字段、查询操作记录、获取工时登记记录列表、获取关联工作项列表、筛选条件查询(MOQL)、查询待办信息、查询人员排期、批量查询交付物信息、获取工作项资源库配置信息、创建工作项资源库、通过资源创建实例
    - **计划表**:创建计划表草稿、编辑计划表草稿、查询计划表草稿行、查询计划表实例行、发布计划表草稿、重置计划表草稿、查询计划表草稿异步操作进度、获取流程资源库元素模板
    - **流程与节点**:获取节点信息、更新节点信息、完成节点、更新子任务、获取获取流转所需必填信息、获取可流转状态、流转状态
    - **视图与度量**:获取视图信息、查询全景视图下的工作项列表、获取度量图表信息、查看图表详情、模糊查询视图、新增固定视图、更新固定视图
    - **评论**:添加评论、查询评论
    - **附件**:上传附件、下载附件
- **开发要点**:
  - 与 Open API 类似,MCP Server 会验证插件是否已申请相关权限,并已在相应空间中安装后才能使用
  - 如果验证失败,服务器会返回具体原因,以便 AI 进行后续处理
  - 不受个人 MCP 开关管控
  - 每个 Tool 的调用频率限制(QPS)为 5
  - 在构建 AI Agent 时,可以在输入信息中提供 Tool 所需参数,来提升 AI 的调用成功率并减少幻觉
- **注意事项**:userKey 需在飞书项目空间 Hover 鼠标至用户头像,然后在名片中双击头像获取

---

## 七、市场插件(ISV)

### 1. 开发并发布一个市场插件

- **核心概念**:ISV 认证租户可以在飞书项目开放平台开发市场插件,并发布版本和设置定价,审核通过后即可被可见租户在插件商店购买使用
- **关键功能/API**:
  - **前提条件**:
    - 成为 ISV 认证租户
    - 完成开发插件环境环境准备
  - **开发流程**:
    1. 创建插件:添加插件、授权接口
    2. 添加构成:选择构成类型、创建客户端开发工程
    3. 设置付费方案:添加付费方案、查看方案 ID
    4. 开发调试客户端(可选)
    5. 开发服务端(可选)
    6. 发布插件:上传版本、发布插件、检查完善发布信息、完成付费设置、完成数据安全检查
  - **付费方案配置**:
    - 方案名称
    - 付费模式:免费、按年付费、一次性收费
    - 可购买企业:所有企业、部分企业(最多 20 个企业 F 码)
    - 每个插件最多添加 1000 个付费方案
  - **试用方案配置**:试用方案名称、试用版本、使用周期(7天、15天、30天、60天)
  - **发布信息**:
    - 基础信息:封面图(必选,1-5张,比例 2:1,不小于 1600px*800px,不超过 10M)、使用手册、服务条款、隐私政策、联系邮箱、联系电话、话题群邀请链接
    - 审核补充材料:插件说明文档、插件自检清单
  - **数据安全检查**:敏感权限列表、已验证权限列表、插件管理权限列表
  - **ISV 认证租户查看插件购买信息**:
    - 订单管理页面
    - 获取租户信息、获取租户安装空间列表、查询买方租户权益信息
- **开发要点**:
  - 非认证租户的市场插件均为免费版本
  - 飞书项目租户内的开发者均可以上传市场插件,但只有认证过 ISV 资质的租户可设置为付费插件
  - 提交完成后,插件状态为待上架,版本状态为审核中,飞书项目团队将在三个工作日内完成审核
  - 非 ISV 认证的租户上架市场插件也需要审核,审核流程包括租户管理员审核、飞书项目平台审核
- **注意事项**:
  - 本功能不支持海外租户和私有化租户使用
  - 新创建的插件默认带有"企业"标签
  - 插件试用需租户管理员授权同意
  - 当插件存在多个付费方案时,租户仅支持唯一方案
  - 一个租户只能试用一次

### 2. 插件购买安装规则

- **试用规则**:
  - 插件试用需租户管理员授权同意,管理员同意后在使用时效期内租户下空间均支持安装试用版本
  - 当租户管理员对插件使用版本产生升级/退订等行为时,针对当前租户下所有已安装插件生效
  - 当租户已经安装免费版插件时,若想试用插件,则所有已安装空间的免费版会被替换为试用版
  - 试用版本到期后不会自动回退到免费版,需要手动操作
  - 一个租户只能试用一次
  - 当租户已购买了插件,且未曾进行过试用,仍不支持试用其他版本

---

## 八、开发者手册完整目录结构

基于从详情标签页文章中提取的左侧导航栏完整目录结构,开发者手册涵盖以下大模块:

### 1. 开发插件
- **什么是插件**:使用场景、术语、快速开始、环境准备、开发并发布第一个插件
- **创建插件**:创建插件、添加插件功能、权限管理、发布插件、安装统计、日志检索
- **设计插件**:Page、View、Tab、Control、Button、Intercept、Connector、Schedule、CustomField、LiteAppComponent、Configuration
- **构建插件客户端**:客户端开发概述、说明、限制、术语、自适应机制、运行模式、脚本模式、开发流程、初始化工程、开发调试、发布产物、工具参考、官方示例
- **构建插件服务端**:Open API 概述、获取访问凭证、x-auth-mode 模式、服务端 SDK、Postman 调用、MCP
- **事件能力**:拦截事件、精细化拦截、按空间设置拦截、查询拦截配置、监听事件、插件装卸监听、兼容性说明与验签说明
- **插件开发教程**:需求分类轻应用组件、排期扩展组件、字段类型组件

### 2. 开发 AI 应用
- **AI 节点**:概念、开发使用、卡片展示、AINode、实现逻辑、Open API、权限说明、数据结构、数据修改、完成节点、获取节点信息、事件
- **AI 字段**:开放能力、开发使用、Open API、更新、事件说明
- **AI 操作**:概念、开发使用、卡片展示、AIOperation、Open API、权限说明、运行状态更新、事件说明
- **AAMP 协议**

### 3. 了解 ISV 服务商体系
- ISV 服务介绍、认证租户、插件管理、开发发布市场插件、更新版本/付费方案、运营数据、质量监控、AI 应用管理、模板管理、解决方案管理、应用管理、交易管理、后台设置

### 4. 集成客户端 API 和组件
- **客户端 API**:简介、通用类型定义、上下文、Context、Page、Tab、Control、Button、Script、View、Configuration、Intercept、Schedule、CustomField、LiteAppComponent
- **通用能力**:containerModal、navigation、storage、clipboard、modal、toast、actionSheet、utils、shared、richTextEditor、event
- **数据获取**:Space、WorkObject、WorkItem、Field
- **客户端组件库**:Popover、Select、DatePicker、UserTag、UserSelect、RichTextEditor、WorkItemFinder

### 5. 接入 Open API
- **格式说明**:数据结构汇总、字段与属性解析、Markdown 格式、搜索参数格式、MQL 语法、错误码
- **API 列表**:用户、空间、工作项、工作项实例读写、工作项工时、工作项资源库、流程与节点、评审管理、子任务、附件、群组、空间关联、文件、配置、评论、视图&度量、租户

### 6. 使用开发工具 CLI
- CLI 介绍、开发实践

### 7. 最佳实践
- 工时资源管理、OKR 集成、Devops X Gitlab 集成、米格会议管理、同步 Jira Issue、容知日新 X 工时管理、文档检索 MCP Server

### 8. 帮助和交流
- 开发者社区、常见问题(使用场景、构成开发、开发调试、历史插件升级、访问凭证、Open API 调用、错误码排查)

### 9. 参考文档
- Semi 设计规范、插件 1.0 手册、插件点位、资源

### 10. JSSDK Reference
- SDKClient、Context、Model(Space/WorkObject/WorkItem/Field)、API(navigation/storage/clipboard/toast)、Enumeration、Interface、Lark Project CLI Reference

---

## 附:成功阅读的文章URL清单

以下是本次实际成功抓取并阅读的飞书项目开发者手册文章URL清单:

### 已成功抓取的文章(17篇)

1. **开发并发布第一个插件**(入口文章)
   - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/nh4exbsn
   - 内容: 插件完整开发生命周期、客户端与服务端数据交互模式、六步开发流程

2. **什么是插件**
   - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/hscnld90
   - 内容: 插件定义、点位构成类型、客户端API/组件/Open API、MCP Server、官方插件类型

3. **创建插件**
   - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/49k1ojm9
   - 内容: 创建插件流程、基本信息配置、协作人管理、删除插件

4. **客户端开发概述**
   - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/zeyhwyhg
   - 内容: 客户端API介绍、客户端组件介绍、客户端限制、开发流程概述

5. **环境准备**
   - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/2ila4b2b
   - 内容: 知识储备、开发客户端环境准备、开发服务端环境准备、SDK安装

6. **开发并发布一个市场插件**
   - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/qn9dbdzw
   - 内容: ISV认证租户、市场插件开发流程、付费方案、发布流程、购买安装规则

7. **插件体系 MCP Server 介绍**
   - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/jdmql9oj
   - 内容: MCP概念、启用MCP Server、MCP Server URL、MCP Tools分类(空间与人员、工作项实例、计划表、流程与节点、视图与度量、评论、附件)

8. **获取访问凭证**
   - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/4id4bvnf
   - 内容: 访问凭证类型、使用方式、获取插件访问凭证、获取用户访问凭证、刷新token、客户端验证

9. **拦截事件 / Intercept**
   - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/1s5ly1az
   - 内容: 拦截事件概念、交互形式、配置项、拦截事件枚举、拦截弹窗、数据通讯能力

10. **详情标签页 / Tab**
    - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/e7darc0m
    - 内容: 详情标签页开发、JSSDK使用、页面绘制、代码示例、完整左侧导航栏目录结构

11. **控件 / Control**
    - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/kui20z6a
    - 内容: 控件概念、适用场景、表单控件开发、表格列控件开发、布局配置、变量申明、物料标签

12. **监听事件**
    - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/lvojfwht
    - 内容: 监听事件概念、监听流程、配置流程、稳定性说明、事件列表、事件结构、各事件详解

13. **自动化连接器 / Connector**
    - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/2cnupe2z
    - 内容: 自动化连接器概念、使用场景、功能范围、功能亮点、触发器开发、操作开发、事件列表

14. **客户端 API 简介**
    - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/x4l57qtx
    - 内容: 客户端API三大类(上下文、通用能力、数据模型)

15. **服务端 SDK**
    - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/3ajzsqlv
    - 内容: SDK优势、语言类型(Golang/Java)、安装方法、SDK版本更新

16. **轻应用组件 / LiteAppComponent**
    - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/bnlgrufr
    - 内容: 轻应用组件概念、开发流程、配置项、组件属性、输出属性、属性分类

17. **使用客户端组件库**
    - URL: https://project.feishu.cn/b/helpcenter/1p8d7djs/aa6btca8
    - 内容: 组件库类型、消费方式、组件库初始化、使用示例、完整代码配置

### 已发现但未深入抓取的文章URL(基于引用)

以下文章URL在抓取过程中被发现,但由于时间和上下文限制未进行深入抓取,但已在大纲中涵盖其核心内容:

- 添加插件功能
- 权限管理
- 发布插件
- 安装统计
- 日志检索
- 导航功能页 / Page
- 视图模式 / View
- 按钮 / Button
- 插件配置 / Configuration
- 字段类型扩展字段: https://project.feishu.cn/b/helpcenter/1p8d7djs/l6963wv2
- 节点排期扩展字段: https://project.feishu.cn/b/helpcenter/1p8d7djs/zmyxzvfb
- 插件配置: https://project.feishu.cn/b/helpcenter/1p8d7djs/2vwtbynb
- 各类按钮: https://project.feishu.cn/b/helpcenter/1p8d7djs/ozbttwpc
- 上下文: https://project.feishu.cn/b/helpcenter/1p8d7djs/zq8fg6bl
- 通用能力: https://project.feishu.cn/b/helpcenter/1p8d7djs/cxj9ivdg
- 数据模型: https://project.feishu.cn/b/helpcenter/1p8d7djs/7fn34lb6
- 成为 ISV 认证租户
- Open API 概述
- API 列表
- 如何处理跨域问题
- Open API 错误码
- Open API 调用咨询
- 开发调试咨询
- 插件访问凭证咨询
- 更新市场插件版本
- 更新付费方案
- 查看付费方案 ID
- 获取租户信息
- 获取租户安装空间列表
- 查询买方租户权益信息
- MQL 语法说明
- 客户端资源限制
- 客户端沙箱限制
- 客户端官方示例插件
- 初始化插件工程
- 开发调试客户端
- 发布插件产物
- 开发客户端环境准备
- 开发服务端环境准备
- 兼容性说明与验签说明
- 精细化拦截
- 按空间设置拦截
- 查询拦截配置
- 插件装卸监听
- x-auth-mode 模式说明
- 使用 postman 调用 API 接口
- 通过插件调用 MCP Server

### 官方插件文章URL

- 工时资源管理插件: https://project.feishu.cn/b/helpcenter/1p8d7djs/bfrmeo1d
- OKR 集成插件: https://project.feishu.cn/b/helpcenter/1p8d7djs/x9qc26hz
- Gitlab 集成插件: https://project.feishu.cn/b/helpcenter/1p8d7djs/zx8t3h7s
- 会议管理插件: https://project.feishu.cn/b/helpcenter/1p8d7djs/df8q378p
- Jira Issue 数据同步插件: https://project.feishu.cn/b/helpcenter/1p8d7djs/4sclunns
- 工时管理插件: https://project.feishu.cn/b/helpcenter/1p8d7djs/fy96q3l0

### 外部资源链接

- quickstart-front 示例代码: https://github.com/larksuite/lark-project-template/tree/v2/quickstart/front
- demo-server 示例代码: https://github.com/larksuite/lark-project-template/tree/v2/quickstart/server
- 字段分类示例工程: https://github.com/larksuite/lark-project-template/tree/feat/help-doc-demo/project/doc-demos/field-category-config
- 工作项管理示例工程: https://github.com/larksuite/lark-project-template/tree/feat/help-doc-demo/project/doc-demos/work-item-manager
- Golang SDK: https://github.com/larksuite/project-oapi-sdk-golang
- Java SDK: https://github.com/larksuite/project-oapi-sdk-java

---

## 未能获取到的内容说明

由于飞书项目帮助中心的页面内容通过 JavaScript 动态加载,分类根URL(https://project.feishu.cn/b/helpcenter/1p8d7djs)无法直接获取到完整的文章列表。以下内容未能深入获取:

1. **Open API 概述**的完整内容(仅从引用中了解)
2. **API 列表**的完整接口清单(涵盖用户、空间、工作项、工作项实例读写、工作项工时、工作项资源库、流程与节点、评审管理、子任务、附件、群组、空间关联、文件、配置、评论、视图&度量、租户等)
3. **客户端 API 各子模块**的详细 API 文档(Context、Page、Tab、Control、Button、Script、View、Configuration、Intercept、Schedule、CustomField、LiteAppComponent)
4. **通用能力各子模块**的详细 API 文档(containerModal、navigation、storage、clipboard、modal、toast、actionSheet、utils、shared、richTextEditor、event)
5. **数据获取各子模块**的详细 API 文档(Space、WorkObject、WorkItem、Field)
6. **AI 应用开发**相关内容(AI 节点、AI 字段、AI 操作、AAMP 协议)
7. **ISV 服务商体系**的详细内容
8. **开发工具 CLI**的详细使用说明
9. **最佳实践**的详细案例
10. **常见问题**的详细解答
11. **JSSDK Reference**的完整 API 参考
12. **插件开发教程**(需求分类轻应用组件、排期扩展组件、字段类型组件)

这些内容已在知识大纲中通过目录结构体现,可通过对应的文章URL进一步抓取获取详细内容。

---

*本知识大纲将作为飞书项目开发者手册的长期知识库基础,可根据需要进一步深入抓取特定主题的详细内容。*
